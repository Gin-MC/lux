---
---
<section id="contacto" class="max-w-7xl mx-auto px-6 py-20 grid md:grid-cols-2 gap-12 bg-black text-yellow-400">

  <!-- FORMULARIO -->
  <form 
    id="formulario-contacto"
    class="space-y-6" 
    name="formulario-contacto"
    aria-labelledby="form-title"
    novalidate
  >
    <h2 id="form-title" class="text-5xl mt-12 mb-4 font-bold pb-1">Cotiza</h2>
    <p class="text-gray-300 text-lg">
      Queremos que tu evento sea inolvidable. Completa el siguiente formulario y recibe una cotización personalizada con todos los detalles que hacen de <span class="text-[#FFD700] font-semibold">LUX</span> la mejor experiencia en coctelería.
    </p>
    <p class="text-gray-400 mb-12 text-sm">
      Nuestro equipo se pondrá en contacto contigo a la brevedad. ¡Salud por los buenos momentos!
    </p>

    <!-- Campos del formulario -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
      <input name="nombre" id="nombre" placeholder="Nombre" required class="form-input" aria-label="Nombre completo" />
      <input type="email" name="email" id="email" placeholder="Correo electrónico" required class="form-input" aria-label="Correo electrónico" />
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
      <input type="tel" name="telefono" id="telefono" placeholder="Teléfono móvil" pattern="[0-9]{9}" required class="form-input" aria-label="Teléfono móvil" />
      <input type="date" name="fecha" id="fecha" required class="form-input" aria-label="Fecha del evento" />
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
      <input name="lugar" id="lugar" placeholder="Lugar del evento" required class="form-input" aria-label="Lugar del evento" />
      <input type="number" name="invitados" id="invitados" placeholder="Número de invitados" min="1" required class="form-input" aria-label="Número de invitados" />
    </div>

    <label for="tipo_evento" class="sr-only">Tipo de evento</label>
    <select name="tipo_evento" id="tipo_evento" required class="form-input" aria-label="Tipo de evento">
      <option value="" disabled selected>Tipo de evento</option>
      <option value="Boda">Boda</option>
      <option value="XV años">XV años</option>
      <option value="Cumpleaños">Cumpleaños</option>
      <option value="Corporativo">Eventos corporativos</option>
      <option value="Promoción">Fiestas de promoción</option>
      <option value="Otros">Otros</option>
    </select>

    <textarea name="mensaje" id="mensaje" rows="4" placeholder="Deja tu mensaje..." class="form-input" aria-label="Mensaje adicional"></textarea>

    <button 
      type="submit" 
      class="bg-[#FFD700] text-black font-bold px-6 py-3 rounded hover:bg-transparent hover:border hover:border-[#FFD700] hover:text-[#FFD700] transition duration-300"
      aria-label="Enviar formulario"
    >
      Enviar mensaje
    </button>
  </form>

  <!-- Información de contacto-->
  <div class="flex flex-col justify-center space-y-6">
    <h2 class="text-3xl font-bold border-b-2 border-[#FFD700] inline-block pb-1">Información de contacto</h2>

    <p class="text-lg text-white font-medium">
      ¿Tienes dudas o quieres algo especial? Estamos aquí para ti.
    </p>

    <ul class="space-y-2 text-gray-400">
      <li class="flex items-center gap-2">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="#ffd700" viewBox="0 0 24 24">
          <path d="M2,6A2,2 0 0,1 4,4H20A2,2 0 0,1 22,6V18A2,2 0 0,1 20,20H4A2,2 0 0,1 2,18V6M4,6V8.17L12,13L20,8.17V6H4M20,18V10.5L12,15.5L4,10.5V18H20Z" />
        </svg>
        <p>luxserviciosbartenders@gmail.com</p>
      </li>
      <li class="flex items-center gap-2">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="#FFD700" viewBox="0 0 24 24">
          <path d="M6.62 10.79a15.053 15.053 0 006.59 6.59l2.2-2.2a1 1 0 011.05-.24c1.12.37 2.33.57 3.54.57a1 1 0 011 1V20a1 1 0 01-1 1A17 17 0 013 4a1 1 0 011-1h3.5a1 1 0 011 1c0 1.21.2 2.42.57 3.54a1 1 0 01-.24 1.05l-2.2 2.2z"/>
        </svg>
        <p>+51 999 999 999</p>
      </li>
      <li class="flex items-center gap-2">
        <svg width="24" height="24" viewBox="0 0 3 2" xmlns="http://www.w3.org/2000/svg">
          <rect width="1" height="2" x="0" y="0" fill="#D91023"/>
          <rect width="1" height="2" x="1" y="0" fill="#ffffff"/>
          <rect width="1" height="2" x="2" y="0" fill="#D91023"/>
        </svg>
        <p>Lima, Perú</p>
      </li>
    </ul>

    <a 
      href="https://wa.me/51999999999" 
      target="_blank" 
      rel="noopener noreferrer"
      class="inline-block mt-2 bg-[#25D366] text-black font-bold px-4 py-2 rounded hover:bg-transparent hover:border hover:border-[#25D366] hover:text-[#25D366] transition duration-300"
      aria-label="Contacto por WhatsApp"
    >
      Escríbenos por WhatsApp
    </a>

    <p class="italic text-sm text-gray-400">
      LUX: Donde cada cóctel cuenta una historia.
    </p>
  </div>
</section>

<style>
  .form-input {
    background-color: #1a1a1a;
    padding: 0.75rem;
    border-radius: 0.5rem;
    border: 1px solid #FFD700;
    color: white;
    width: 100%;
  }

  input[type="number"]::-webkit-inner-spin-button,
  input[type="number"]::-webkit-outer-spin-button {
    -webkit-appearance: none;
    margin: 0;
  }

  input[type="number"] {
    -moz-appearance: textfield;
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    margin: -1px;
    padding: 0;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    border: 0;
  }
</style>

<script type="module" src="/scripts/formSubmit.js"></script>
