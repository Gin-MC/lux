---
const { clientes } = Astro.props;
---

<table class="w-full text-left text-sm text-gray-300 mt-6">
  <thead class="bg-[#1f1f1f] text-[#5CD2C6]">
    <tr>
      <th class="px-4 py-2">Nombre</th>
      <th class="px-4 py-2">Email</th>
      <th class="px-4 py-2">Último Evento</th>
    </tr>
  </thead>
  <tbody>
    {clientes.map(cliente => (
      <tr class="border-b border-gray-700 hover:bg-[#2a2a2a]">
        <td class="px-4 py-2">{cliente.nombre}</td>
        <td class="px-4 py-2">{cliente.email}</td>
        <td class="px-4 py-2">{cliente.ultimoEvento}</td>
      </tr>
    ))}
  </tbody>
</table>
